name: Deploy to EC2

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  # 先进行代码检查
  check:
    name: 代码检查
    runs-on: ubuntu-latest

    steps:
    - name: 📦 检出代码
      uses: actions/checkout@v4

    - name: 🟢 设置 Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'
        cache: 'npm'

    - name: 📥 安装依赖
      run: npm ci

    - name: 🔍 TypeScript 编译检查
      run: npx tsc --noEmit

  # 检查通过后才部署
  deploy:
    name: 部署到服务器
    needs: check  # ← 关键：依赖 check job
    runs-on: ubuntu-latest

    steps:
    - name: 📦 Checkout code
      uses: actions/checkout@v4

    - name: 📋 Show commit info
      run: |
        echo "部署提交: ${{ github.sha }}"
        echo "提交者: ${{ github.actor }}"
        echo "提交信息: ${{ github.event.head_commit.message }}"

    - name: 🚀 Deploy to EC2
      uses: appleboy/ssh-action@v1.0.0
      with:
        host: ${{ secrets.EC2_HOST }}
        username: ${{ secrets.EC2_USER }}
        key: ${{ secrets.EC2_SSH_KEY }}
        script: |
          /home/ubuntu/deploy-qqbot.sh

    - name: ✅ Deployment complete
      run: |
        echo "🎉 QQ Bot 已成功部署到服务器！"
        echo "查看日志: docker compose logs -f bot"
