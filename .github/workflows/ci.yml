name: CI/CD - 检查与部署

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  check:
    name: TypeScript 编译检查
    runs-on: ubuntu-latest
    env:
      DATABASE_URL: "file:./dev.db"

    steps:
    - name: 📦 检出代码
      uses: actions/checkout@v4

    - name: 🟢 设置 Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'
        cache: 'npm'

    - name: 📥 安装 Bot 依赖
      run: npm ci

    - name: 🔧 生成 Prisma Client
      run: npx prisma generate

    - name: 🔍 TypeScript 编译检查 - Bot 主程序
      run: npx tsc --noEmit

    - name: 📥 安装 Admin Server 依赖
      working-directory: admin/server
      run: npm ci

    - name: 🔍 TypeScript 编译检查 - Admin Server
      working-directory: admin/server
      run: npx tsc --noEmit

    - name: 📥 安装 Admin Web 依赖
      working-directory: admin/web
      run: npm ci

    - name: 🔍 TypeScript 编译检查 - Admin Web
      working-directory: admin/web
      run: npx vue-tsc --noEmit

    - name: ✅ 检查通过
      run: echo "🎉 所有代码检查通过！"

  deploy:
    name: 部署到 EC2
    needs: check
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest

    steps:
    - name: 📦 检出代码
      uses: actions/checkout@v4

    - name: 📋 显示提交信息
      run: |
        echo "部署提交: ${{ github.sha }}"
        echo "提交者: ${{ github.actor }}"
        echo "提交信息: ${{ github.event.head_commit.message }}"

    - name: 🚀 部署到 EC2
      uses: appleboy/ssh-action@v1.0.0
      with:
        host: ${{ secrets.EC2_HOST }}
        username: ${{ secrets.EC2_USER }}
        key: ${{ secrets.EC2_SSH_KEY }}
        script: |
          /home/ubuntu/deploy-qqbot.sh

    - name: ✅ 部署完成
      run: |
        echo "🎉 QQ Bot 已成功部署到服务器！"
        echo "查看日志: docker compose logs -f bot"
