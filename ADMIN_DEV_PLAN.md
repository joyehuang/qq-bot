# QQ Bot 管理后台开发计划

## 📋 项目概述

### 项目目标
为 QQ 打卡机器人开发一个 Web 管理后台，方便管理员查看打卡记录、用户数据、统计分析等信息。

### 核心功能
- 管理员登录认证
- 打卡记录查询（分页、筛选、搜索、删除）
- 用户列表和详情
- 数据统计和可视化
- 数据导出（CSV）
- 暗黑模式支持

---

## 🛠️ 技术栈

### 后端 API
- **框架**: Express.js 4.18+
- **语言**: TypeScript 5.0+
- **认证**: JWT (jsonwebtoken)
- **验证**: zod
- **数据库**: Prisma Client (共享 Bot 数据库)
- **跨域**: cors
- **安全**: helmet
- **日志**: morgan

### 前端
- **框架**: Vue 3.4+ (Composition API)
- **构建工具**: Vite 5.0+
- **语言**: TypeScript 5.0+
- **UI 组件库**: Element Plus 2.4+
- **状态管理**: Pinia 2.1+
- **路由**: Vue Router 4.2+
- **HTTP 客户端**: axios 1.6+
- **图表库**: ECharts 5.4+ (vue-echarts)
- **日期处理**: dayjs
- **CSS**: Element Plus 内置 + Scoped CSS

### 部署
- **容器化**: Docker + Docker Compose
- **反向代理**: Nginx
- **域名**:
  - 前端: `admin.joyehuang.me`
  - API: `admin-api.joyehuang.me`
- **HTTPS**: Let's Encrypt

---

## 📁 项目结构

```
qq-bot/
├── src/                                # Bot 主程序（已有）
│   └── index.ts
│
├── admin/                              # 管理后台（新增）
│   ├── server/                         # 后端 API
│   │   ├── src/
│   │   │   ├── index.ts               # 服务器入口
│   │   │   ├── config/                # 配置
│   │   │   │   └── env.ts             # 环境变量
│   │   │   ├── routes/                # 路由模块
│   │   │   │   ├── index.ts           # 路由汇总
│   │   │   │   ├── auth.ts            # 认证路由
│   │   │   │   ├── checkins.ts        # 打卡记录路由
│   │   │   │   ├── users.ts           # 用户路由
│   │   │   │   └── stats.ts           # 统计路由
│   │   │   ├── middleware/            # 中间件
│   │   │   │   ├── auth.ts            # JWT 认证中间件
│   │   │   │   ├── error.ts           # 错误处理中间件
│   │   │   │   └── validator.ts       # 参数验证中间件
│   │   │   ├── utils/                 # 工具函数
│   │   │   │   ├── jwt.ts             # JWT 工具
│   │   │   │   ├── response.ts        # 统一响应格式
│   │   │   │   └── pagination.ts      # 分页工具
│   │   │   └── types/                 # 类型定义
│   │   │       └── index.ts
│   │   ├── package.json
│   │   └── tsconfig.json
│   │
│   └── web/                            # 前端
│       ├── public/
│       │   └── favicon.ico
│       ├── src/
│       │   ├── main.ts                # 入口文件
│       │   ├── App.vue                # 根组件
│       │   ├── router/                # 路由配置
│       │   │   └── index.ts
│       │   ├── stores/                # Pinia 状态管理
│       │   │   ├── user.ts            # 用户状态
│       │   │   └── app.ts             # 应用状态
│       │   ├── views/                 # 页面组件
│       │   │   ├── Login.vue          # 登录页
│       │   │   ├── Layout.vue         # 布局容器
│       │   │   ├── Dashboard.vue      # 仪表盘
│       │   │   ├── Checkins.vue       # 打卡记录
│       │   │   └── Users.vue          # 用户管理
│       │   ├── components/            # 公共组件
│       │   │   ├── Navbar.vue         # 顶部导航
│       │   │   ├── Sidebar.vue        # 侧边栏
│       │   │   └── Charts/            # 图表组件
│       │   │       ├── LineChart.vue  # 折线图
│       │   │       ├── PieChart.vue   # 饼图
│       │   │       └── BarChart.vue   # 柱状图
│       │   ├── api/                   # API 接口
│       │   │   ├── auth.ts
│       │   │   ├── checkins.ts
│       │   │   ├── users.ts
│       │   │   └── stats.ts
│       │   ├── utils/                 # 工具函数
│       │   │   ├── request.ts         # axios 封装
│       │   │   └── format.ts          # 格式化工具
│       │   └── types/                 # 类型定义
│       │       └── index.ts
│       ├── index.html
│       ├── package.json
│       ├── vite.config.ts
│       ├── tsconfig.json
│       └── .env.development
│
├── prisma/                             # 数据库（共享）
│   ├── schema.prisma
│   └── generated/
│
├── nginx/                              # Nginx 配置（新增）
│   └── admin.conf
│
├── .env                                # 环境变量（更新）
├── docker-compose.yml                  # Docker 配置（更新）
├── package.json                        # 根 package.json
└── ADMIN_DEV_PLAN.md                   # 本文档
```

---

## 🗄️ 数据库设计

### 现有表（复用）

#### users 表
```prisma
model User {
  id              Int           @id @default(autoincrement())
  qqNumber        String        @unique
  nickname        String
  streakDays      Int           @default(0)
  maxStreak       Int           @default(0)
  lastCheckinDate DateTime?
  dailyGoal       Int?
  aiStyle         String        @default("encourage")
  createdAt       DateTime      @default(now())
  updatedAt       DateTime      @updatedAt
  checkins        Checkin[]
  achievements    Achievement[]
}
```

#### checkins 表
```prisma
model Checkin {
  id          Int      @id @default(autoincrement())
  userId      Int
  groupId     String
  duration    Int
  content     String
  isLoan      Boolean  @default(false)
  category    String?
  subcategory String?
  createdAt   DateTime @default(now())
  user        User     @relation(fields: [userId], references: [id])
}
```

#### suggestions 表
```prisma
model Suggestion {
  id        Int      @id @default(autoincrement())
  qqNumber  String
  nickname  String
  groupId   String
  content   String
  status    String   @default("pending")
  createdAt DateTime @default(now())
}
```

---

## 🔌 API 接口设计

### 基础配置
- **Base URL**: `http://localhost:3001` (开发) / `https://admin-api.joyehuang.me` (生产)
- **认证方式**: JWT Bearer Token (除登录接口外都需要)
- **响应格式**: JSON
- **时区**: Asia/Shanghai

### 统一响应格式

#### 成功响应
```json
{
  "success": true,
  "data": { ... }
}
```

#### 分页响应
```json
{
  "success": true,
  "data": {
    "items": [ ... ],
    "total": 100,
    "page": 1,
    "pageSize": 20,
    "totalPages": 5
  }
}
```

#### 错误响应
```json
{
  "success": false,
  "error": {
    "message": "错误信息",
    "code": "ERROR_CODE"
  }
}
```

---

### 1. 认证接口

#### POST /api/auth/login
登录接口

**请求体**:
```json
{
  "username": "admin",
  "password": "password"
}
```

**成功响应** (200):
```json
{
  "success": true,
  "data": {
    "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
    "expiresIn": 604800,
    "username": "admin"
  }
}
```

**失败响应** (401):
```json
{
  "success": false,
  "error": {
    "message": "用户名或密码错误",
    "code": "INVALID_CREDENTIALS"
  }
}
```

#### POST /api/auth/verify
验证 Token 是否有效

**请求头**:
```
Authorization: Bearer <token>
```

**成功响应** (200):
```json
{
  "success": true,
  "data": {
    "valid": true,
    "username": "admin"
  }
}
```

---

### 2. 打卡记录接口

#### GET /api/checkins
获取打卡记录列表（分页）

**查询参数**:
- `page`: 页码（默认 1）
- `pageSize`: 每页数量（默认 20，最大 100）
- `userId`: 用户 ID（可选，筛选指定用户）
- `startDate`: 开始日期（可选，格式 YYYY-MM-DD）
- `endDate`: 结束日期（可选，格式 YYYY-MM-DD）
- `category`: 分类（可选）
- `isLoan`: 是否贷款打卡（可选，true/false）
- `keyword`: 搜索关键词（可选，搜索打卡内容）
- `sortBy`: 排序字段（可选，createdAt/duration，默认 createdAt）
- `sortOrder`: 排序方向（可选，asc/desc，默认 desc）

**示例请求**:
```
GET /api/checkins?page=1&pageSize=20&startDate=2025-01-01&category=学习
```

**成功响应** (200):
```json
{
  "success": true,
  "data": {
    "items": [
      {
        "id": 1,
        "userId": 1,
        "user": {
          "id": 1,
          "qqNumber": "123456",
          "nickname": "张三"
        },
        "groupId": "789",
        "duration": 120,
        "content": "学习 Vue 3",
        "isLoan": false,
        "category": "学习",
        "subcategory": "计算机·前端",
        "createdAt": "2025-12-15T10:30:00Z"
      }
    ],
    "total": 100,
    "page": 1,
    "pageSize": 20,
    "totalPages": 5
  }
}
```

#### GET /api/checkins/:id
获取单条打卡记录详情

**成功响应** (200):
```json
{
  "success": true,
  "data": {
    "id": 1,
    "userId": 1,
    "user": {
      "id": 1,
      "qqNumber": "123456",
      "nickname": "张三",
      "streakDays": 5,
      "aiStyle": "encourage"
    },
    "groupId": "789",
    "duration": 120,
    "content": "学习 Vue 3",
    "isLoan": false,
    "category": "学习",
    "subcategory": "计算机·前端",
    "createdAt": "2025-12-15T10:30:00Z"
  }
}
```

#### DELETE /api/checkins/:id
删除打卡记录

**成功响应** (200):
```json
{
  "success": true,
  "data": {
    "message": "删除成功"
  }
}
```

#### GET /api/checkins/export
导出打卡记录为 CSV

**查询参数**: 同 GET /api/checkins

**成功响应** (200):
```
Content-Type: text/csv
Content-Disposition: attachment; filename="checkins_20251215.csv"

ID,用户,QQ号,内容,时长,分类,日期
1,张三,123456,学习Vue3,120分钟,学习,2025-12-15 10:30:00
...
```

---

### 3. 用户接口

#### GET /api/users
获取用户列表（分页）

**查询参数**:
- `page`: 页码（默认 1）
- `pageSize`: 每页数量（默认 20）
- `keyword`: 搜索关键词（可选，搜索昵称或 QQ 号）
- `sortBy`: 排序字段（可选，createdAt/streakDays，默认 createdAt）
- `sortOrder`: 排序方向（可选，asc/desc，默认 desc）

**成功响应** (200):
```json
{
  "success": true,
  "data": {
    "items": [
      {
        "id": 1,
        "qqNumber": "123456",
        "nickname": "张三",
        "streakDays": 5,
        "maxStreak": 10,
        "lastCheckinDate": "2025-12-15T10:30:00Z",
        "dailyGoal": 120,
        "aiStyle": "encourage",
        "createdAt": "2025-01-01T00:00:00Z",
        "_count": {
          "checkins": 50
        },
        "totalDuration": 6000
      }
    ],
    "total": 50,
    "page": 1,
    "pageSize": 20,
    "totalPages": 3
  }
}
```

#### GET /api/users/:id
获取用户详情

**成功响应** (200):
```json
{
  "success": true,
  "data": {
    "id": 1,
    "qqNumber": "123456",
    "nickname": "张三",
    "streakDays": 5,
    "maxStreak": 10,
    "lastCheckinDate": "2025-12-15T10:30:00Z",
    "dailyGoal": 120,
    "aiStyle": "encourage",
    "createdAt": "2025-01-01T00:00:00Z",
    "_count": {
      "checkins": 50,
      "achievements": 3
    },
    "stats": {
      "totalDuration": 6000,
      "totalCheckins": 50,
      "averageDuration": 120,
      "categoryBreakdown": {
        "学习": 4000,
        "工作": 1500,
        "运动": 500
      }
    },
    "recentCheckins": [
      {
        "id": 100,
        "content": "学习 Vue 3",
        "duration": 120,
        "createdAt": "2025-12-15T10:30:00Z"
      }
    ]
  }
}
```

#### GET /api/users/:id/checkins
获取用户的打卡记录

**查询参数**: 同 GET /api/checkins（自动筛选该用户）

**成功响应**: 同 GET /api/checkins

---

### 4. 统计接口

#### GET /api/stats/overview
获取总览统计

**成功响应** (200):
```json
{
  "success": true,
  "data": {
    "totalUsers": 100,
    "totalCheckins": 5000,
    "totalDuration": 600000,
    "today": {
      "checkins": 50,
      "duration": 6000,
      "users": 30
    },
    "thisWeek": {
      "checkins": 300,
      "duration": 36000,
      "users": 80
    },
    "thisMonth": {
      "checkins": 1200,
      "duration": 144000,
      "users": 95
    }
  }
}
```

#### GET /api/stats/trend
获取打卡趋势数据

**查询参数**:
- `period`: 时间周期（day/week/month，默认 week）
- `limit`: 返回数据点数量（默认 30）

**成功响应** (200):
```json
{
  "success": true,
  "data": {
    "period": "day",
    "data": [
      {
        "date": "2025-12-01",
        "checkins": 45,
        "duration": 5400,
        "users": 25
      },
      {
        "date": "2025-12-02",
        "checkins": 50,
        "duration": 6000,
        "users": 30
      }
    ]
  }
}
```

#### GET /api/stats/category
获取分类统计

**查询参数**:
- `startDate`: 开始日期（可选）
- `endDate`: 结束日期（可选）

**成功响应** (200):
```json
{
  "success": true,
  "data": {
    "categories": [
      {
        "category": "学习",
        "count": 2000,
        "duration": 240000,
        "percentage": 40
      },
      {
        "category": "工作",
        "count": 1500,
        "duration": 180000,
        "percentage": 30
      }
    ],
    "subcategories": [
      {
        "subcategory": "计算机·前端",
        "category": "学习",
        "count": 800,
        "duration": 96000,
        "percentage": 16
      }
    ]
  }
}
```

#### GET /api/stats/leaderboard
获取排行榜

**查询参数**:
- `period`: 时间周期（today/week/month/all，默认 week）
- `limit`: 返回人数（默认 10）

**成功响应** (200):
```json
{
  "success": true,
  "data": {
    "period": "week",
    "leaderboard": [
      {
        "rank": 1,
        "userId": 1,
        "qqNumber": "123456",
        "nickname": "张三",
        "checkins": 10,
        "duration": 1200,
        "streakDays": 7
      }
    ]
  }
}
```

---

## 🎨 前端页面设计

### 1. 登录页 (Login.vue)

**路由**: `/login`

**功能**:
- 用户名密码输入
- 记住我（可选）
- 登录按钮
- 错误提示

**布局**:
```
┌─────────────────────────────┐
│                             │
│        QQ Bot 管理后台       │
│                             │
│   ┌─────────────────────┐   │
│   │ 用户名              │   │
│   └─────────────────────┘   │
│   ┌─────────────────────┐   │
│   │ 密码                │   │
│   └─────────────────────┘   │
│   □ 记住我                  │
│   ┌─────────────────────┐   │
│   │      登录           │   │
│   └─────────────────────┘   │
└─────────────────────────────┘
```

### 2. 布局容器 (Layout.vue)

**路由**: `/` (需要认证)

**组件结构**:
- 顶部导航栏 (Navbar)
- 左侧边栏 (Sidebar)
- 主内容区 (RouterView)

**布局**:
```
┌────────────────────────────────────────┐
│ Logo    导航       暗黑模式  用户  登出 │ ← Navbar
├──────┬─────────────────────────────────┤
│      │                                 │
│ 菜单 │         主内容区                │
│      │                                 │
│ 导航 │    <router-view />              │
│      │                                 │
│ 栏   │                                 │
│      │                                 │
└──────┴─────────────────────────────────┘
  ↑
 Sidebar
```

### 3. 仪表盘 (Dashboard.vue)

**路由**: `/`

**功能**:
- 总览统计卡片（用户数、打卡数、总时长）
- 今日/本周/本月统计
- 打卡趋势图（折线图）
- 分类占比（饼图）
- 最近打卡记录列表（前 10 条）
- 排行榜（前 5 名）

**布局**:
```
┌─────────────────────────────────────────────┐
│ 统计卡片                                     │
│ ┌────────┐ ┌────────┐ ┌────────┐ ┌────────┐│
│ │总用户  │ │总打卡  │ │总时长  │ │今日    ││
│ │  100   │ │ 5000   │ │10000h  │ │50次    ││
│ └────────┘ └────────┘ └────────┘ └────────┘│
├─────────────────────────────────────────────┤
│ 图表区域                                     │
│ ┌──────────────────────┐ ┌─────────────────┐│
│ │  打卡趋势（折线图）   │ │ 分类占比（饼图）││
│ │                      │ │                 ││
│ └──────────────────────┘ └─────────────────┘│
├─────────────────────────────────────────────┤
│ ┌──────────────────────┐ ┌─────────────────┐│
│ │  最近打卡            │ │  本周排行榜     ││
│ │  - 张三 学习2h       │ │  1. 张三 12h    ││
│ │  - 李四 运动1h       │ │  2. 李四 10h    ││
│ └──────────────────────┘ └─────────────────┘│
└─────────────────────────────────────────────┘
```

### 4. 打卡记录 (Checkins.vue)

**路由**: `/checkins`

**功能**:
- 筛选区域（日期范围、用户、分类、是否贷款）
- 搜索框（搜索打卡内容）
- 排序（按时间、时长）
- 打卡记录表格（分页）
  - ID、用户、QQ号、内容、时长、分类、日期、操作
- 操作按钮（查看详情、删除）
- 导出 CSV 按钮
- 分页器

**布局**:
```
┌─────────────────────────────────────────────┐
│ 打卡记录                                     │
├─────────────────────────────────────────────┤
│ 筛选：                                       │
│ [日期范围] [用户] [分类] [是否贷款]          │
│ 搜索：[________]  排序：[时间▼]  [导出CSV]  │
├─────────────────────────────────────────────┤
│ ID │用户│QQ号│内容│时长│分类│日期│操作      │
│ 1  │张三│123 │Vue │2h  │学习│12/15│查看删除│
│ 2  │李四│456 │跑步│1h  │运动│12/15│查看删除│
│ ...                                         │
├─────────────────────────────────────────────┤
│         < 1 2 3 4 5 >  共 100 条            │
└─────────────────────────────────────────────┘
```

### 5. 用户管理 (Users.vue)

**路由**: `/users`

**功能**:
- 搜索框（搜索昵称或 QQ 号）
- 排序（按注册时间、连续天数）
- 用户列表表格（分页）
  - ID、QQ号、昵称、连续天数、总打卡、总时长、注册时间、操作
- 操作按钮（查看详情）
- 详情对话框（弹窗显示用户详细信息）
  - 基本信息
  - 统计数据
  - 分类占比图
  - 最近打卡记录

**布局**:
```
┌─────────────────────────────────────────────┐
│ 用户管理                                     │
├─────────────────────────────────────────────┤
│ 搜索：[________]  排序：[注册时间▼]          │
├─────────────────────────────────────────────┤
│ ID│QQ号│昵称│连续│总打卡│总时长│注册时间│操作│
│ 1 │123 │张三│7天 │50次  │100h  │2025/1/1│查看│
│ 2 │456 │李四│3天 │30次  │60h   │2025/1/2│查看│
│ ...                                         │
├─────────────────────────────────────────────┤
│         < 1 2 3 4 5 >  共 50 条             │
└─────────────────────────────────────────────┘
```

---

## 📝 开发步骤

### 阶段 1：项目基础搭建（预计 30 分钟）

#### 任务清单

- [ ] 1.1 创建项目目录结构
  - [ ] 创建 `admin/server/` 目录
  - [ ] 创建 `admin/web/` 目录
  - [ ] 创建 `nginx/` 目录

- [ ] 1.2 后端项目初始化
  - [ ] 创建 `admin/server/package.json`
  - [ ] 创建 `admin/server/tsconfig.json`
  - [ ] 安装后端依赖
    - express
    - typescript
    - @types/express
    - @types/node
    - cors
    - @types/cors
    - helmet
    - morgan
    - @types/morgan
    - jsonwebtoken
    - @types/jsonwebtoken
    - zod
    - dotenv
    - ts-node
    - nodemon

- [ ] 1.3 前端项目初始化
  - [ ] 使用 Vite 创建 Vue 3 + TypeScript 项目
  - [ ] 安装前端依赖
    - element-plus
    - @element-plus/icons-vue
    - pinia
    - vue-router
    - axios
    - echarts
    - vue-echarts
    - dayjs

- [ ] 1.4 环境变量配置
  - [ ] 更新根目录 `.env` 文件
    - `ADMIN_USERNAME=admin`
    - `ADMIN_PASSWORD=your-secure-password-here`
    - `JWT_SECRET=your-jwt-secret-key-here`
    - `ADMIN_API_PORT=3001`
  - [ ] 创建 `admin/web/.env.development`
    - `VITE_API_BASE_URL=http://localhost:3001/api`
  - [ ] 创建 `admin/web/.env.production`
    - `VITE_API_BASE_URL=https://admin-api.joyehuang.me/api`

---

### 阶段 2：后端 API 开发（预计 3-4 小时）

#### 任务清单

- [ ] 2.1 基础设施
  - [ ] 创建 `admin/server/src/config/env.ts`（环境变量加载）
  - [ ] 创建 `admin/server/src/types/index.ts`（类型定义）
  - [ ] 创建 `admin/server/src/utils/response.ts`（统一响应格式）
  - [ ] 创建 `admin/server/src/utils/pagination.ts`（分页工具）

- [ ] 2.2 JWT 认证
  - [ ] 创建 `admin/server/src/utils/jwt.ts`
    - `generateToken()` - 生成 JWT
    - `verifyToken()` - 验证 JWT
  - [ ] 创建 `admin/server/src/middleware/auth.ts`
    - `authMiddleware` - 验证 JWT 中间件

- [ ] 2.3 错误处理
  - [ ] 创建 `admin/server/src/middleware/error.ts`
    - `errorHandler` - 全局错误处理中间件

- [ ] 2.4 参数验证
  - [ ] 创建 `admin/server/src/middleware/validator.ts`
    - `validate()` - zod 验证中间件工厂函数

- [ ] 2.5 认证路由
  - [ ] 创建 `admin/server/src/routes/auth.ts`
    - `POST /api/auth/login` - 登录
    - `POST /api/auth/verify` - 验证 Token

- [ ] 2.6 打卡记录路由
  - [ ] 创建 `admin/server/src/routes/checkins.ts`
    - `GET /api/checkins` - 获取打卡列表（分页、筛选、搜索）
    - `GET /api/checkins/:id` - 获取打卡详情
    - `DELETE /api/checkins/:id` - 删除打卡
    - `GET /api/checkins/export` - 导出 CSV

- [ ] 2.7 用户路由
  - [ ] 创建 `admin/server/src/routes/users.ts`
    - `GET /api/users` - 获取用户列表（分页、搜索）
    - `GET /api/users/:id` - 获取用户详情（含统计）
    - `GET /api/users/:id/checkins` - 获取用户的打卡记录

- [ ] 2.8 统计路由
  - [ ] 创建 `admin/server/src/routes/stats.ts`
    - `GET /api/stats/overview` - 总览统计
    - `GET /api/stats/trend` - 打卡趋势
    - `GET /api/stats/category` - 分类统计
    - `GET /api/stats/leaderboard` - 排行榜

- [ ] 2.9 路由汇总
  - [ ] 创建 `admin/server/src/routes/index.ts`
    - 汇总所有路由

- [ ] 2.10 服务器入口
  - [ ] 创建 `admin/server/src/index.ts`
    - Express 应用初始化
    - 中间件配置
    - 路由注册
    - 错误处理
    - 启动服务器

- [ ] 2.11 测试
  - [ ] 使用 Postman/curl 测试所有 API 接口
  - [ ] 确保返回格式正确
  - [ ] 确保错误处理正常

---

### 阶段 3：前端基础开发（预计 2-3 小时）

#### 任务清单

- [ ] 3.1 项目配置
  - [ ] 配置 `vite.config.ts`
    - 路径别名 `@/` → `src/`
    - 代理配置（开发环境）
  - [ ] 配置 `tsconfig.json`

- [ ] 3.2 全局样式和主题
  - [ ] 在 `main.ts` 中引入 Element Plus
  - [ ] 配置暗黑模式支持

- [ ] 3.3 类型定义
  - [ ] 创建 `src/types/index.ts`
    - 定义 API 响应类型
    - 定义数据模型类型（User, Checkin 等）

- [ ] 3.4 HTTP 客户端
  - [ ] 创建 `src/utils/request.ts`
    - axios 实例配置
    - 请求拦截器（添加 Token）
    - 响应拦截器（错误处理、401 跳转登录）

- [ ] 3.5 工具函数
  - [ ] 创建 `src/utils/format.ts`
    - `formatDuration()` - 格式化时长
    - `formatDate()` - 格式化日期
    - `formatNumber()` - 格式化数字

- [ ] 3.6 API 接口定义
  - [ ] 创建 `src/api/auth.ts`
    - `login()` - 登录
    - `verifyToken()` - 验证 Token
  - [ ] 创建 `src/api/checkins.ts`
    - `getCheckins()` - 获取打卡列表
    - `getCheckinDetail()` - 获取打卡详情
    - `deleteCheckin()` - 删除打卡
    - `exportCheckins()` - 导出 CSV
  - [ ] 创建 `src/api/users.ts`
    - `getUsers()` - 获取用户列表
    - `getUserDetail()` - 获取用户详情
    - `getUserCheckins()` - 获取用户打卡记录
  - [ ] 创建 `src/api/stats.ts`
    - `getOverview()` - 获取总览统计
    - `getTrend()` - 获取趋势数据
    - `getCategoryStats()` - 获取分类统计
    - `getLeaderboard()` - 获取排行榜

- [ ] 3.7 状态管理
  - [ ] 创建 `src/stores/user.ts`（用户状态）
    - `token` - JWT Token
    - `username` - 用户名
    - `login()` - 登录方法
    - `logout()` - 登出方法
    - `checkAuth()` - 检查登录状态
  - [ ] 创建 `src/stores/app.ts`（应用状态）
    - `isDark` - 暗黑模式状态
    - `toggleDark()` - 切换暗黑模式
    - `sidebarCollapsed` - 侧边栏折叠状态
    - `toggleSidebar()` - 切换侧边栏

- [ ] 3.8 路由配置
  - [ ] 创建 `src/router/index.ts`
    - 路由定义（Login, Layout, Dashboard, Checkins, Users）
    - 路由守卫（检查登录状态）
    - 重定向逻辑

- [ ] 3.9 根组件
  - [ ] 更新 `src/App.vue`
    - RouterView
    - Element Plus 全局配置
    - 暗黑模式 CSS 类绑定

---

### 阶段 4：前端页面开发（预计 4-5 小时）

#### 任务清单

- [ ] 4.1 登录页
  - [ ] 创建 `src/views/Login.vue`
    - 页面布局（居中卡片）
    - 表单（用户名、密码）
    - 登录按钮（loading 状态）
    - 错误提示
    - 登录成功跳转

- [ ] 4.2 布局组件
  - [ ] 创建 `src/views/Layout.vue`
    - 使用 Element Plus Container 布局
    - 包含 Navbar 和 Sidebar
    - RouterView 主内容区
  - [ ] 创建 `src/components/Navbar.vue`
    - Logo 和标题
    - 面包屑导航（可选）
    - 右侧：暗黑模式切换、用户名、登出按钮
  - [ ] 创建 `src/components/Sidebar.vue`
    - 菜单导航（Dashboard、打卡记录、用户管理）
    - 折叠按钮
    - 高亮当前路由

- [ ] 4.3 仪表盘页面
  - [ ] 创建 `src/views/Dashboard.vue`
    - 统计卡片（使用 Element Plus Card）
      - 总用户数
      - 总打卡数
      - 总时长
      - 今日打卡
    - 创建 `src/components/Charts/LineChart.vue`（打卡趋势）
    - 创建 `src/components/Charts/PieChart.vue`（分类占比）
    - 最近打卡列表（使用 Element Plus Table）
    - 排行榜列表

- [ ] 4.4 打卡记录页面
  - [ ] 创建 `src/views/Checkins.vue`
    - 筛选区域（使用 Element Plus Form）
      - 日期范围选择器
      - 用户选择器（搜索）
      - 分类选择器
      - 是否贷款选择器
      - 重置按钮
    - 搜索框
    - 排序选择器
    - 导出 CSV 按钮
    - 打卡记录表格（Element Plus Table）
      - 列定义（ID、用户、内容、时长、分类、日期）
      - 操作列（查看、删除按钮）
      - loading 状态
    - 分页器（Element Plus Pagination）
    - 删除确认对话框
    - 查看详情对话框（可选）

- [ ] 4.5 用户管理页面
  - [ ] 创建 `src/views/Users.vue`
    - 搜索框
    - 排序选择器
    - 用户列表表格（Element Plus Table）
      - 列定义（ID、QQ号、昵称、连续天数、总打卡、总时长、注册时间）
      - 操作列（查看详情按钮）
    - 分页器
    - 用户详情对话框（Dialog）
      - 基本信息展示
      - 统计数据
      - 创建 `src/components/Charts/BarChart.vue`（分类占比柱状图）
      - 最近打卡记录表格

---

### 阶段 5：功能完善和优化（预计 2-3 小时）

#### 任务清单

- [ ] 5.1 暗黑模式
  - [ ] 在 `stores/app.ts` 中实现暗黑模式持久化（localStorage）
  - [ ] 在 `App.vue` 中根据状态切换 Element Plus 暗黑模式
  - [ ] 测试所有页面在暗黑模式下的显示效果

- [ ] 5.2 响应式设计
  - [ ] 测试并优化移动端显示（768px 以下）
  - [ ] 侧边栏在移动端自动折叠
  - [ ] 表格在移动端使用卡片布局（可选）

- [ ] 5.3 错误处理
  - [ ] 统一错误提示（使用 Element Plus Message）
  - [ ] 网络错误提示
  - [ ] 401 自动跳转登录
  - [ ] 404 页面（可选）

- [ ] 5.4 加载状态
  - [ ] 所有数据加载添加 loading 状态
  - [ ] 按钮点击添加 loading 状态
  - [ ] 页面切换添加过渡动画（可选）

- [ ] 5.5 数据刷新
  - [ ] 添加手动刷新按钮
  - [ ] 列表操作后自动刷新

- [ ] 5.6 表单验证
  - [ ] 登录表单验证
  - [ ] 筛选表单验证

- [ ] 5.7 代码优化
  - [ ] 提取公共组件
  - [ ] 提取公共逻辑（composables）
  - [ ] 添加必要的注释

---

### 阶段 6：部署配置（预计 1-2 小时）

#### 任务清单

- [ ] 6.1 Docker 配置
  - [ ] 创建 `admin/server/Dockerfile`
    - Node.js 镜像
    - 安装依赖
    - 构建 TypeScript
    - 运行服务
  - [ ] 创建 `admin/web/Dockerfile`
    - Node.js 镜像（构建阶段）
    - Nginx 镜像（运行阶段）
    - 构建前端
    - 配置 Nginx

- [ ] 6.2 Docker Compose 配置
  - [ ] 更新 `docker-compose.yml`
    - 添加 `admin-api` 服务
    - 添加 `admin-web` 服务
    - 配置网络
    - 配置环境变量
    - 配置卷挂载

- [ ] 6.3 Nginx 配置
  - [ ] 创建 `nginx/admin.conf`
    - 前端静态文件服务（admin.joyehuang.me）
    - API 反向代理（admin-api.joyehuang.me）
    - HTTPS 配置（待 Let's Encrypt 证书生成后）
    - Gzip 压缩
    - 缓存策略

- [ ] 6.4 域名和 HTTPS
  - [ ] 在域名提供商添加 DNS 记录
    - `admin.joyehuang.me` → 服务器 IP
    - `admin-api.joyehuang.me` → 服务器 IP
  - [ ] 使用 Certbot 生成 Let's Encrypt 证书
  - [ ] 配置 Nginx HTTPS
  - [ ] 配置 HTTP 自动跳转 HTTPS

- [ ] 6.5 GitHub Actions（可选）
  - [ ] 创建 `.github/workflows/deploy-admin.yml`
    - 触发条件：push to main，且改动在 admin/ 目录
    - 构建前端
    - 构建后端
    - SSH 到服务器部署

---

### 阶段 7：测试和上线（预计 1 小时）

#### 任务清单

- [ ] 7.1 本地测试
  - [ ] 后端所有 API 接口测试
  - [ ] 前端所有页面功能测试
  - [ ] 登录登出流程测试
  - [ ] 暗黑模式切换测试
  - [ ] 响应式布局测试（不同屏幕尺寸）
  - [ ] 错误处理测试（网络错误、Token 过期等）

- [ ] 7.2 生产环境部署
  - [ ] 更新服务器 `.env` 文件（生产环境配置）
  - [ ] 构建并启动 Docker 容器
  - [ ] 验证服务启动成功
  - [ ] 验证域名访问
  - [ ] 验证 HTTPS 证书

- [ ] 7.3 生产环境测试
  - [ ] 通过域名访问前端
  - [ ] 测试登录功能
  - [ ] 测试所有页面功能
  - [ ] 测试移动端访问
  - [ ] 性能测试（加载速度）

- [ ] 7.4 文档更新
  - [ ] 更新 README.md
    - 添加管理后台使用说明
    - 添加部署说明
  - [ ] 更新 COMMANDS.md（如需要）

---

## 🔧 环境配置文件

### .env（根目录，更新）

```env
# 现有配置保持不变...

# 管理后台配置（新增）
ADMIN_USERNAME=admin
ADMIN_PASSWORD=change-this-to-secure-password
JWT_SECRET=change-this-to-random-secret-key
ADMIN_API_PORT=3001
```

### admin/web/.env.development

```env
VITE_API_BASE_URL=http://localhost:3001/api
```

### admin/web/.env.production

```env
VITE_API_BASE_URL=https://admin-api.joyehuang.me/api
```

---

## 🚀 运行命令

### 开发环境

```bash
# 后端 API
cd admin/server
npm install
npm run dev

# 前端
cd admin/web
npm install
npm run dev

# Bot 主程序（原有）
npm run dev
```

### 生产环境

```bash
# 构建并启动所有服务
docker-compose up -d --build

# 只重启管理后台服务
docker-compose up -d --build admin-api admin-web
```

---

## 📊 进度追踪

### 预计总时间：15-20 小时

- ✅ 阶段 1：项目基础搭建（0.5h）
- ✅ 阶段 2：后端 API 开发（3-4h）
- ✅ 阶段 3：前端基础开发（2-3h）
- ✅ 阶段 4：前端页面开发（4-5h）
- ✅ 阶段 5：功能完善和优化（2-3h）
- ✅ 阶段 6：部署配置（1-2h）
- ✅ 阶段 7：测试和上线（1h）

---

## 🎯 成功标准

### 功能完成标准
- [ ] 管理员可以通过域名访问管理后台
- [ ] 登录认证正常工作
- [ ] 可以查看、搜索、筛选、删除打卡记录
- [ ] 可以查看用户列表和详情
- [ ] 可以查看统计数据和图表
- [ ] 可以导出数据为 CSV
- [ ] 暗黑模式正常工作
- [ ] 移动端访问正常

### 性能标准
- [ ] 首屏加载时间 < 3 秒
- [ ] API 响应时间 < 500ms
- [ ] 图表渲染流畅

### 安全标准
- [ ] JWT Token 认证正常
- [ ] HTTPS 配置正确
- [ ] 敏感信息不在前端暴露
- [ ] SQL 注入防护（Prisma 自动防护）
- [ ] XSS 防护（Vue 自动转义）

---

## 📝 注意事项

1. **安全**
   - 务必修改默认的 `ADMIN_PASSWORD` 和 `JWT_SECRET`
   - 不要在前端代码中硬编码敏感信息
   - 生产环境必须使用 HTTPS

2. **性能**
   - 打卡记录列表使用分页，避免一次加载过多数据
   - 图表数据做好缓存
   - 静态资源使用 CDN（可选）

3. **兼容性**
   - 主要支持现代浏览器（Chrome, Firefox, Safari, Edge）
   - 移动端基础功能可用即可

4. **可维护性**
   - 代码结构清晰，职责分明
   - 关键逻辑添加注释
   - API 接口文档完整

5. **扩展性**
   - 预留后续功能扩展接口
   - 组件设计考虑复用性

---

## 🔄 后续优化方向（可选）

1. **功能增强**
   - [ ] 批量删除打卡记录
   - [ ] 打卡记录编辑功能
   - [ ] 用户信息编辑功能
   - [ ] 建议管理功能
   - [ ] 更多统计维度（时段分析、同比环比等）

2. **体验优化**
   - [ ] 页面骨架屏
   - [ ] 虚拟滚动（超长列表）
   - [ ] 图表动画效果
   - [ ] 更多图表类型

3. **性能优化**
   - [ ] 前端资源懒加载
   - [ ] 图片压缩和优化
   - [ ] API 响应缓存
   - [ ] CDN 加速

4. **架构升级**
   - [ ] 迁移到 NestJS（如需要）
   - [ ] 增加单元测试
   - [ ] 增加 E2E 测试

---

## ✅ 开发清单

开发时按照此文档的阶段顺序执行，完成一项勾选一项。

开始时间：2025-12-15
预计完成时间：2025-12-17

---

**准备好开始开发了吗？** 🚀
